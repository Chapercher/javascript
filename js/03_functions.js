// Création d'une fonction avec paramètre optionnel

/*
const message = function(text = 'texte par défault'){
	alert(text);
}
message('Avec paramètre');
*/

// Création d'une fonction fléchée

const somme = (a,b) => {
	alert('Résultat : ' + (a + b));
};

somme(10,2);
